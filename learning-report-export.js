(function(){'use strict';const clone=v=>JSON.parse(JSON.stringify(v));function report(userId,type='summary'){const all=window.RupaiLearningInsights.summary(userId),payload=type==='progress'?{analytics:all.analytics,trend:all.trend,strongest:all.strongest,weakest:all.weakest,goals:all.goals,streak:all.streak}:type==='revision'?all.revision:{totalStudySeconds:all.totalStudySeconds,lessonsCompleted:all.lessonsCompleted,revisionCount:all.revisionCount,quiz:all.quiz,goals:all.goals,streak:all.streak};return{schema:'rupai-learning-report',version:1,type,exportedAt:new Date().toISOString(),localOnly:true,userId,report:clone(payload)}}function json(userId,type){return JSON.stringify(report(userId,type),null,2)}function download(userId,type){const blob=new Blob([json(userId,type)],{type:'application/json'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=`rupai-${type}-report-${new Date().toISOString().slice(0,10)}.json`;a.click();setTimeout(()=>URL.revokeObjectURL(url),0);return a.download}window.RupaiLearningReportExport={report,json,download};})();
