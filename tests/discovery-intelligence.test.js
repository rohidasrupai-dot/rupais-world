const assert=require('assert'),fs=require('fs'),vm=require('vm'),{webcrypto}=require('crypto');class Storage{constructor(){this.d={}}getItem(k){return this.d[k]??null}setItem(k,v){this.d[k]=String(v)}}const localStorage=new Storage(),navigator={onLine:false},window={localStorage,navigator,crypto:webcrypto,dispatchEvent(){},addEventListener(){}},context=vm.createContext({window,localStorage,navigator,crypto:webcrypto,structuredClone,console,CustomEvent:function(){}});for(const file of ['studio/store.js','studio/learning-memory-service.js','studio/knowledge-graph-service.js','studio/learning-path-service.js','studio/curiosity-engine-service.js','studio/discovery-intelligence-service.js'])vm.runInContext(fs.readFileSync(file,'utf8'),context,{filename:file});
const S=window.TeachCurioStore,G=window.TeachCurioKnowledgeGraph,P=window.TeachCurioLearningPaths,C=window.TeachCurioCuriosity,D=window.TeachCurioDiscoveryIntelligence,creator={userId:'creator_discovery',permissions:['curio.brain.use','curio.brain.review','curio.rules.manage','curio.decision.audit']},learner={userId:'learner_discovery',permissions:['curio.brain.use']};
function seed(){const s=S.read();s.projects.push({id:'ancient',title:'Ancient India',subject:'History',status:'structure_ready'},{id:'maps_lesson',title:'Maps and Rivers',subject:'Geography',status:'structure_ready'});s.lessonStructures.push({id:'ancient_structure',projectId:'ancient',approvalStatus:'approved',nodes:[{id:'stone_d',nodeType:'concept',title:'Stone Age',verificationStatus:'user_approved'},{id:'farming_d',nodeType:'concept',title:'Agriculture',verificationStatus:'user_approved'}]},{id:'maps_structure',projectId:'maps_lesson',approvalStatus:'approved',nodes:[{id:'map_d',nodeType:'concept',title:'Maps',verificationStatus:'user_approved'}]});s.memoryEvents.push({id:'m1',studentId:learner.userId,type:'lesson_learned',projectId:'ancient',title:'Ancient India',evidence:{subject:'History',format:'story',firstChoice:true},createdAt:'2026-07-28T10:00:00.000Z'},{id:'m2',studentId:learner.userId,type:'lesson_learned',projectId:'maps_lesson',title:'Maps',evidence:{subject:'Geography',format:'map',firstChoice:true},createdAt:'2026-07-29T10:00:00.000Z'},{id:'m3',studentId:learner.userId,type:'lesson_learned',projectId:'maps_lesson',title:'River Map',evidence:{subject:'Geography',format:'map'},createdAt:'2026-07-30T10:00:00.000Z'},{id:'m4',studentId:learner.userId,type:'revision_completed',projectId:'ancient',title:'Stone Age revision',evidence:{subject:'History',format:'revision'},createdAt:'2026-07-31T10:00:00.000Z'},{id:'m5',studentId:learner.userId,type:'quiz_completed',projectId:'ancient',title:'History quiz',evidence:{subject:'History',format:'quiz'},createdAt:'2026-08-01T10:00:00.000Z'});s.curioLearningSessions.push({id:'session_d',studentId:learner.userId,projectId:'ancient',status:'paused',createdAt:'2026-08-01T11:00:00.000Z'});s.weakTopics.push({id:'weak_d',studentId:learner.userId,topic:'Stone Age',status:'needs_revision'});s.revisionSchedules.push({id:'revision_d',studentId:learner.userId,conceptId:'stone_d',title:'Stone Age',status:'pending',reason:'Recorded incorrect recall.',dueAt:'2026-08-02T00:00:00.000Z'});s.quizAttempts.push({id:'quiz_d',studentId:learner.userId,quizId:'q1'});s.visualAssets.push({id:'animation_d',assetType:'Animation',title:'Agriculture animation',status:'approved'});s.visualActivityAttempts.push({id:'visual_d',studentId:learner.userId,visualType:'map',firstChoice:true});S.write(s);G.sync(creator);G.addRelationship({from:'Stone Age',to:'Agriculture',type:'prerequisite',evidence:'Approved lesson order places Stone Age before Agriculture.'},creator);P.saveGoal(learner.userId,{type:'school_learning',targetNodeId:'Agriculture'},learner);P.generate({studentId:learner.userId,target:'Agriculture',level:'beginner'},learner)}seed();
const graph=G.list(creator),stone=graph.nodes.find(x=>x.title==='Stone Age'),farm=graph.nodes.find(x=>x.title==='Agriculture'),relation=graph.relationships.find(x=>x.fromNodeId===stone.id&&x.toNodeId===farm.id),card=C.createCard({type:'explore_more',title:'From Stone to Farming',prompt:'How did farming change life?',explanation:'Approved evidence places Agriculture after Stone Age learning.',subject:'History',topic:'Agriculture',interestTags:['history','ancient india'],evidenceReferenceIds:[relation.id],status:'approved'},creator);C.interact(learner.userId,card.id,'completed',learner);C.interact(learner.userId,card.id,'favorite',learner);
const memory=D.memory(learner.userId,learner);assert.ok(memory.frequentlyExploredSubjects.some(x=>x.label==='History'));assert.ok(memory.favoriteLearningFormats.some(x=>x.format==='map'));assert.ok(memory.completedDiscoveryIds.includes(card.id));assert.equal(memory.observableEvidenceOnly,true);assert.equal(memory.hiddenMemory,false);
let interest=D.interests(learner.userId,learner);assert.ok(interest.some(x=>x.label==='History'));assert.ok(interest.every(x=>x.evidenceCount>0&&/recorded local/.test(x.explanation)));let s=S.read();s.memoryEvents.push({id:'m6',studentId:learner.userId,type:'lesson_learned',projectId:'ancient',title:'More History',evidence:{subject:'History',format:'story',firstChoice:true},createdAt:'2026-08-02T10:00:00.000Z'});S.write(s);interest=D.interests(learner.userId,learner);assert.equal(interest.find(x=>x.label==='History').trend,'growing');
const styles=D.styles(learner.userId,learner);assert.ok(styles.some(x=>x.format==='map'&&x.confidence!=='insufficient'));assert.ok(styles.every(x=>/no emotion or psychological profiling/i.test(x.inferenceBoundary)));const recommendations=D.recommend(learner.userId,learner),types=new Set(recommendations.map(x=>x.type));assert.ok(types.has('continue_learning'));assert.ok(types.has('review_again'));assert.ok(types.has('explore_related_topic'));assert.ok(types.has('try_visual_lesson'));assert.ok(types.has('practice_quiz'));assert.ok(types.has('watch_animation'));assert.ok(recommendations.every(x=>x.optional&&x.providerUsed===false&&x.evidenceIds));
const timeline=D.timeline(learner.userId,learner);assert.ok(timeline.some(x=>x.type==='revision_completed'));assert.ok(timeline.some(x=>x.type==='discovery_unlocked'));assert.ok(timeline.some(x=>x.type==='milestone'));assert.ok(timeline.every(x=>x.createdAt));const approved=D.review(recommendations[0].id,'creator_approved','Verified local evidence.',creator);assert.equal(approved.status,'creator_approved');assert.throws(()=>D.review(recommendations[1].id,'creator_approved','',learner),/Creator review/);
const dash=D.dashboard(learner.userId,creator);assert.equal(D.review(dash.recommendations[0].id,'creator_approved','Dashboard review persistence.',creator).status,'creator_approved');assert.ok(dash.strongestInterests.length);assert.ok(dash.growingInterests.length);assert.equal(dash.weakAreas[0].topic,'Stone Age');assert.equal(dash.revisionNeeds[0].conceptId,'stone_d');assert.ok(dash.recommendedNextPath);assert.ok(dash.timeline.length);assert.equal(dash.transparency.observableBehaviorOnly,true);assert.equal(dash.transparency.emotionsInferred,false);assert.equal(dash.transparency.psychologyProfiled,false);assert.equal(dash.transparency.consciousnessClaimed,false);assert.equal(dash.transparency.hiddenMemories,false);assert.equal(dash.provider.configured,false);assert.match(D.providerStatus().message,/recorded local learning behavior only/);assert.throws(()=>D.memory(learner.userId,{userId:'other',permissions:['curio.brain.use']}),/only your own/);
console.log('Discovery Intelligence tests passed: discovery memory, evidence-based interest growth, observable format detection, seven recommendation families, progress/revision/weak-topic/curiosity/path integration, discovery timeline, creator review, transparent dashboard, offline/no-provider safety and authorization.');
