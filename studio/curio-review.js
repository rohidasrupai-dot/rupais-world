(function(){'use strict';const C=TeachCurioConversation,$=q=>document.querySelector(q),session=window.__reviewSession,esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),label=v=>String(v||'').replaceAll('_',' ').replace(/\b\w/g,x=>x.toUpperCase());function toast(t){const x=$('#toast');x.textContent=t;x.classList.add('show');setTimeout(()=>x.classList.remove('show'),1800)}function render(){const rows=C.reviewQueue({status:$('#status').value||null});$('#summary').textContent=`${rows.length} review item(s)`;$('#queue').innerHTML=rows.map(r=>`<article class="review"><div class="review-head"><div><small>${label(r.reason)} · ${label(r.priority)} priority</small><h2>${esc(r.detail||'Review Curio response')}</h2></div><span>${new Date(r.createdAt).toLocaleString()}</span></div>${r.message?`<blockquote>${esc(r.message.content)}</blockquote><small>${esc(r.message.trustLabel||r.message.verificationState)}</small>`:''}<div class="actions">${['approve_conversation_only','convert_to_lesson_draft','edit','add_source','reject','archive','create_content_task'].map(a=>`<button data-id="${r.id}" data-action="${a}">${label(a)}</button>`).join('')}</div></article>`).join('')||'<p>No items match this filter.</p>';document.querySelectorAll('[data-id]').forEach(b=>b.onclick=()=>{C.resolveReview(b.dataset.id,session.userId,{action:b.dataset.action,notes:'Resolved in creator review workspace.'});toast('Review updated.');render()})}$('#refresh').onclick=render;$('#status').onchange=render;render()})();
