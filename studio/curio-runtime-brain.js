(function(){'use strict';const button=document.querySelector('#brainRuntimeSuggest'),reason=document.querySelector('#brainRuntimeReason'),params=new URLSearchParams(location.search),projectId=params.get('project'),session=window.__studioSession;button?.addEventListener('click',()=>{const project=TeachCurioStore.getProject(projectId),runtime=window.TeachCurioRuntime?.latest?.(projectId),concept=runtime?.currentStep?.title||project?.title||'',actor={userId:session.userId,permissions:session.permissions||[]};try{const out=TeachCurioBrainDecision.decide({studentId:session.userId,sessionId:runtime?.session?.id||null,projectId,concept,approvedEvidence:project?[{id:project.id,title:project.title,summary:'Current creator-approved runtime lesson context'}]:[],offline:navigator.onLine===false},actor);reason.textContent=`${out.decision.recommendedAction.replaceAll('_',' ')} · ${out.decision.confidence.replaceAll('_',' ')} confidence. ${out.decision.reason}`}catch(error){reason.textContent=error.message}})})();
