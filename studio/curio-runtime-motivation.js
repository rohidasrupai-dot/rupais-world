(function(){'use strict';const session=window.__studioSession,params=new URLSearchParams(location.search),projectId=params.get('project'),box=document.querySelector('#runtimeMotivation'),M=window.TeachCurioMotivation;if(!M||!box)return;const runtime=window.TeachCurioRuntime?.latest?.(projectId),actor={userId:session.userId,permissions:session.permissions||[]};if(runtime?.session?.status==='completed'){M.record(session.userId,{type:'lesson_completed',lessonId:projectId,evidenceId:runtime.session.id,verified:true},actor);const result=M.milestones(session.userId,{reducedMotion:matchMedia('(prefers-reduced-motion: reduce)').matches},actor),latest=result.newItems.at(-1),celebration=latest?TeachCurioStore.read().motivationCelebrations.find(x=>x.milestoneId===latest.id):null;box.textContent=celebration?.message||M.support(session.userId,{context:'difficult_lesson_completed',evidenceIds:[runtime.session.id]},actor).message}else box.textContent=M.support(session.userId,{context:'persistence'},actor).message})();
